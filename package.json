{
  "name": "theme-gaia",
  "version": "1.0.2", 
  "main": "./dist/index.js",     
  "module": "./dist/index.mjs",  
  "types": "./dist/index.d.ts",  
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",  
      "import": "./dist/index.mjs",
      "require": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsup src/index.ts --format cjs,esm --dts --clean",
    "prepare": "npm run build",
    "release": "git checkout build && git merge main --no-edit && npm run build && git add dist -f && (git diff-index --quiet HEAD || git commit -m \"chore: new release\") && git push origin build && git checkout main && echo '🚀 Publicado exitosamente!'"
  },
  "peerDependencies": {
    "react": ">=17",
    "@mui/material": ">=5",
    "@emotion/react": ">=11",
    "@emotion/styled": ">=11"
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "typescript": "^5.0.0" 
  }
}